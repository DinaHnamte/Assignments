export interface Form_Data {
  name: string;
  email: string;
  phone: string;
}

export interface api_data {
  user_id: number;
  id: number;
  title: string;
  body: string;
}

export interface Column {
  field: string;
  headerName: string;
}
